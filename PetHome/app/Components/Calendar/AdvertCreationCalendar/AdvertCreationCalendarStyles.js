import { StyleSheet } from 'react-native'
import Colors from '../../../Constants/Colors';

const AdvertCreationCalendarStyles = StyleSheet.create({
    applyButton: {
        backgroundColor: Colors.green,
        padding: 10,
        alignItems: 'center',
        borderRadius: 5,
    }
});

export default AdvertCreationCalendarStyles;